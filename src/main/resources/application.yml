server:
  port: 8091
spring:
  #   base de datos posta
  datasource:
    url: jdbc:mysql://localhost:3307/politics
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    open-in-view: false
    show-sql: true
    hibernate:
      format_sql: true
      ddl-auto: create-drop

  batch:
    job:
      enabled: false
    initialize-schema: ALWAYS

file:
  input: zonas.csv

